extends layout

block content

  h1  #{iteminstance.item.name}
  h2  Store: #{iteminstance.location.name} 
  h2  Price: #{iteminstance.item.price}
  h2  Description: #{iteminstance.item.description}
  h3  Product Id: #{iteminstance._id}

  div(style='margin-left:20px;margin-top:20px')


    hr
    p
      a(href=iteminstance.url+'/delete') Delete Instance
    p
      a(href=iteminstance.url+'/update') Update Instance